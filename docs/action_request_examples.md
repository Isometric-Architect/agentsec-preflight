# Action Request Examples

Action requests show the user intent, descriptor reference, requested action, argument summary, approval status, expected decision, reason codes, safe next action, and blocked action.

They are designed for parser tests and report review. No request calls a live tool or connects to a runtime.
