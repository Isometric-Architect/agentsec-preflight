# Runtime Recon Plan

OpenClaw-like, Hermes-like, and MCP runtimes are future preflight/index/receipt targets.

This pack does not download, clone, install, run, connect, authenticate, or execute those runtimes. Future recon starts with static docs/source review and requires separate approval before any read-only clone, install, sandbox run, or live execution.

Runtime fixture work should convert actions into local descriptors first, then evaluate them with AgentSec Preflight.
