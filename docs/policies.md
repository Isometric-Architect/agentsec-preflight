# Runtime Policies

Policy files live in `policies/`:

- `openclaw_style_default.yaml`
- `hermes_style_default.yaml`
- `mcp_default_policy.yaml`

They map synthetic action surfaces to PASS/HOLD/BLOCK route expectations. They are local policy examples only and do not approve external action.
